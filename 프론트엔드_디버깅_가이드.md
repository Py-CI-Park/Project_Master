# 프론트엔드 하얀 화면 문제 해결 가이드

**작성일**: 2025-11-06
**시스템**: 폐쇄망 프로젝트 관리 시스템 v2.0.0

---

## 🔍 하얀 화면 문제 증상

프론트엔드 서버(http://localhost:3099)에 접속 시 하얀 화면만 표시되는 경우

---

## 📋 원인 진단 방법

### 1단계: 브라우저 개발자 도구 확인

**Chrome/Edge**:
1. `F12` 키 또는 `Ctrl+Shift+I` 누르기
2. **Console** 탭 확인
3. **Network** 탭에서 실패한 요청 확인

**주요 확인 사항**:
- JavaScript 오류 메시지
- 실패한 네트워크 요청 (빨간색 표시)
- CORS 오류
- 모듈 로딩 실패

### 2단계: 일반적인 원인 체크리스트

#### ✅ Backend 서버 상태
```bash
# Backend 서버가 실행 중인지 확인
curl http://localhost:8099/health
```

**예상 응답**: `{"status":"healthy"}`

**문제 발생 시**:
- Backend 서버 미실행 → `run_backend.bat` 실행
- 포트 충돌 → 배치 파일이 자동으로 처리

#### ✅ Frontend 서버 로그 확인
```bash
# Terminal에서 Vite 서버 로그 확인
```

**정상 로그**:
```
VITE v7.1.12  ready in 1207 ms
➜  Local:   http://localhost:3099/
➜  Network: http://172.23.22.139:3099/
```

**오류 로그**:
- 모듈 import 실패
- 포트 사용 중 오류
- 컴파일 에러

#### ✅ API 통신 확인

**브라우저 Console에서 실행**:
```javascript
// Backend API 연결 테스트
fetch('http://localhost:8099/health')
  .then(res => res.json())
  .then(data => console.log('Backend OK:', data))
  .catch(err => console.error('Backend Error:', err));
```

**CORS 오류 발생 시**:
- `backend/app/main.py`의 CORS 설정 확인
- 포트 3099가 `allow_origins`에 포함되어 있는지 확인

---

## 🛠️ 일반적인 해결 방법

### 해결책 1: 서버 재시작

**Backend 재시작**:
```cmd
1. Ctrl+C로 Backend 서버 종료
2. backend 폴더의 run_backend.bat 재실행
3. http://localhost:8099/health 접속 확인
```

**Frontend 재시작**:
```cmd
1. Ctrl+C로 Frontend 서버 종료
2. frontend 폴더의 run_frontend.bat 재실행
3. http://localhost:3099 접속 확인
```

### 해결책 2: 브라우저 캐시 삭제

**Chrome/Edge**:
1. `Ctrl+Shift+Del` 키 누르기
2. "캐시된 이미지 및 파일" 체크
3. "데이터 삭제" 클릭
4. 페이지 새로고침 (`Ctrl+F5`)

**또는 시크릿 모드 테스트**:
- `Ctrl+Shift+N` (시크릿 모드)
- http://localhost:3099 접속
- 정상 작동 시 → 캐시 문제 확정

### 해결책 3: 환경 변수 확인

**`.env.development` 파일 확인**:
```bash
# frontend/.env.development
VITE_API_BASE_URL=http://localhost:8099
```

**주의사항**:
- `VITE_API_BASE_URL`에 `/api/v1` 경로를 포함하지 말 것
- `.env` 파일 수정 후 반드시 서버 재시작

### 해결책 4: 모듈 재설치

**Frontend 모듈 문제 의심 시**:
```cmd
cd frontend

# node_modules 삭제
rmdir /s /q node_modules
del /f /q package-lock.json

# 재설치
install_frontend.bat
```

### 해결책 5: 포트 충돌 해결

**포트 사용 중 확인**:
```cmd
# 포트 8099 확인
netstat -ano | findstr ":8099"

# 포트 3099 확인
netstat -ano | findstr ":3099"
```

**프로세스 강제 종료**:
```cmd
# PID 확인 후 종료
taskkill /F /PID <PID번호>
```

---

## 🔧 고급 디버깅

### JavaScript Console Errors

#### 1. "Failed to load module script"
**원인**: Vite 빌드 문제 또는 경로 오류

**해결**:
```cmd
cd frontend
npm run build    # 프로덕션 빌드 테스트
npm run dev      # 개발 서버 재시작
```

#### 2. "Cannot read property of undefined"
**원인**: 초기 데이터 로딩 실패

**해결**:
1. Backend 서버 연결 확인
2. 브라우저 Network 탭에서 API 호출 확인
3. ErrorBoundary에서 오류 메시지 확인

#### 3. "404 Not Found"
**원인**: React Router 경로 문제

**해결**:
1. http://localhost:3099/ 접속 (루트 경로)
2. `src/routes/index.tsx` 확인
3. 라우팅 설정 검증

#### 4. CORS Policy Error
**원인**: Backend CORS 설정 누락

**해결**:
```python
# backend/app/main.py
allow_origins=[
    "http://localhost:3099",  # ← 이 줄 확인
    "http://localhost:3000",
    "http://localhost:5173",
],
```

### Network Tab 분석

#### 정상 로딩 순서:
1. `/ `(HTML) - 200 OK
2. `/@vite/client` - 200 OK
3. `/src/main.tsx` - 200 OK
4. React 모듈들 - 200 OK
5. `/health` (Backend API) - 200 OK

#### 비정상 패턴:
- 많은 404 오류 → 경로 문제
- 500 오류 → Backend 서버 문제
- CORS 오류 → CORS 설정 문제
- Timeout → Backend 미실행

---

## 📊 시스템 상태 점검

### 필수 점검 항목

| 항목 | 확인 방법 | 정상 상태 |
|------|----------|----------|
| Backend 서버 | http://localhost:8099/health | `{"status":"healthy"}` |
| Frontend 서버 | Terminal 로그 확인 | `VITE v7.1.12 ready` |
| API 통신 | 브라우저 Console | CORS 오류 없음 |
| 포트 사용 | `netstat -ano \| findstr ":8099"` | Backend PID |
| 포트 사용 | `netstat -ano \| findstr ":3099"` | Frontend PID |
| 환경 변수 | `.env.development` | `VITE_API_BASE_URL=http://localhost:8099` |

### 정상 작동 확인

**1. Backend 확인**:
```bash
curl http://localhost:8099/health
# 응답: {"status":"healthy"}

curl http://localhost:8099/
# 응답: {"message":"폐쇄망 프로젝트 관리 시스템 API","version":"2.0.0",...}
```

**2. Frontend 확인**:
```bash
curl http://localhost:3099
# 응답: <!doctype html> ... (HTML 페이지)
```

**3. 브라우저 확인**:
- http://localhost:3099 접속
- 로딩 화면 표시 후 로그인 페이지 또는 대시보드 표시
- 브라우저 Console에 JavaScript 오류 없음

---

## 🐛 특정 오류 메시지별 해결책

### "Cannot find module"
```
Error: Cannot find module '@mui/material'
```

**해결**:
```cmd
cd frontend
npm install @mui/material @emotion/react @emotion/styled
```

### "localStorage is not defined"
```
ReferenceError: localStorage is not defined
```

**원인**: SSR 환경에서 localStorage 접근

**해결**: 이 프로젝트는 클라이언트 전용이므로 정상 작동해야 함. 다른 원인 확인 필요.

### "i18n not initialized"
```
Error: i18next:: i18n.t does not exist.
```

**원인**: i18n 초기화 실패

**해결**:
```cmd
cd frontend
npm install i18next react-i18next i18next-browser-languagedetector
```

---

## 💡 예방 조치

### 1. 항상 Backend 먼저 실행
```cmd
1. backend/run_backend.bat
2. "Application startup complete" 확인
3. frontend/run_frontend.bat
```

### 2. 포트 번호 일관성 유지
- Backend: 항상 8099
- Frontend: 항상 3099
- 변경 시 모든 설정 파일 동시 수정

### 3. 정기적인 dependency 업데이트
```cmd
# Backend
cd backend
pip list --outdated

# Frontend
cd frontend
npm outdated
```

---

## 📞 추가 도움이 필요한 경우

### 로그 수집

**Backend 로그**:
- Terminal 출력 전체 복사
- `backend/data/` 폴더 확인

**Frontend 로그**:
- Terminal 출력 전체 복사
- 브라우저 Console 전체 복사 (에러 메시지 포함)
- 브라우저 Network 탭 스크린샷

### 시스템 정보

```cmd
# Python 버전
python --version

# Node.js 버전
node --version

# npm 버전
npm --version

# OS 정보 (Windows)
systeminfo | findstr /B /C:"OS Name" /C:"OS Version"
```

---

**참고 문서**:
- `배치파일_사용법.md` - 배치 파일 사용 가이드
- `실행방법.md` - 시스템 실행 가이드
- `TEST_RESULTS.md` - 테스트 결과 및 알려진 문제

**마지막 업데이트**: 2025-11-06
**버전**: v2.0.0
