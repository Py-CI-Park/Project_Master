# 하얀 화면 문제 해결 요약 보고서

**날짜**: 2025-11-06
**버전**: v2.0.0
**최종 상태**: ✅ 로그인 성공, ⚠️ API 호출 오류 발견

---

## 📋 해결한 문제들

### 1. ✅ 포트 변경 완료
- Backend: 8000 → **8099**
- Frontend: 3000 → **3099**, **3100**
- CORS 설정에 포트 3100 추가

### 2. ✅ axios 타입 오류 해결
- `InternalAxiosRequestConfig` → `any` 타입으로 변경
- axios 1.13.1 버전 호환성 문제 해결

### 3. ✅ 로그인 성공
- Backend 로그: `POST /api/v1/auth/login HTTP/1.1" 200 OK`
- 사용자 인증 정상 작동

---

## ⚠️ 발견 및 해결된 문제들

### 문제 1: 401 Unauthorized (인증 토큰 전달 안됨) ✅ 해결
```
GET http://localhost:8099/api/v1/notifications/ 401 (Unauthorized)
```

**원인**: axios 인터셉터가 localStorage에서 토큰을 읽었으나 authStore에 저장됨

**해결**:
- `frontend/src/config/axiosConfig.ts`에서 `useAuthStore.getState().accessToken` 사용하도록 수정
- 인증 토큰이 정상적으로 API 요청에 포함됨

### 문제 2: 404 Not Found (API 경로 오류) ✅ 해결
```
GET http://localhost:8099/projects/ 404 (Not Found)
```

**원인**: `axiosConfig.ts`의 baseURL에 `/api/v1` prefix가 없었음

**해결**:
- `frontend/src/config/axiosConfig.ts`의 baseURL을 `${API_BASE_URL}/api/v1`로 수정

### 문제 3: Double /api/v1 Prefix ✅ 해결
```
GET http://localhost:8099/api/v1/api/v1/notifications/ 404 (Not Found)
```

**원인**:
- axiosConfig baseURL에 `/api/v1` 포함
- notificationService에서도 `/api/v1/notifications/` 경로 사용
- 결과: `/api/v1/api/v1/notifications/` (중복)

**해결**:
- `frontend/src/services/notificationService.ts`의 모든 경로에서 `/api/v1` prefix 제거
- `/api/v1/notifications/` → `/notifications/`
- `/api/v1/notifications/unread-count` → `/notifications/unread-count`
- `/api/v1/notifications/${id}/read` → `/notifications/${id}/read`
- `/api/v1/notifications/read-all` → `/notifications/read-all`

---

## 🔧 남은 문제 및 검증 필요

### 남은 문제: 422 Unprocessable Entity (프로젝트 생성 오류)
```
POST http://localhost:8099/api/v1/projects/ 422 (Unprocessable Entity)
```

**원인 분석 필요**:
- 백엔드 유효성 검증 오류
- 필수 필드 누락 또는 데이터 형식 오류 가능성

**다음 단계**:
1. ProjectForm에서 전송하는 데이터 확인
2. 백엔드 API 스키마와 비교
3. 필수 필드 및 데이터 타입 검증

### 전체 기능 검증 필요
- ✅ 로그인/로그아웃
- ✅ API 경로 및 인증 토큰 전달
- ⚠️ 프로젝트 CRUD (생성 오류 확인 필요)
- ⏳ 태스크 CRUD
- ⏳ Enabler CRUD
- ⏳ 알림 기능
- ⏳ 의존성 관리
- ⏳ WebSocket 연결

---

**작성**: Claude Code
**최종 업데이트**: 2025-11-06 13:50 KST
