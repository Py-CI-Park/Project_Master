# 하얀 화면 문제 해결 완료

**작성일**: 2025-11-06
**버전**: v2.0.0
**포트**: Backend 8099, Frontend 3099

---

## 📋 문제 분석

### 증상
- Frontend 접속 시 하얀 화면만 표시됨
- 로그인 페이지가 렌더링되지 않음
- 브라우저 Console에서 특정 오류 없음

### 근본 원인

**Zustand 인증 스토어의 상태 복원 문제**

1. **문제점**:
   ```typescript
   // authStore.ts (수정 전)
   isAuthenticated: false,  // 초기값이 항상 false
   isLoading: false,        // 초기값이 false
   ```

2. **발생 메커니즘**:
   - 사용자가 페이지 접속 → React 앱 초기화
   - Zustand persist 미들웨어가 localStorage에서 토큰 복원 시작
   - **하지만**: `isAuthenticated`는 복원되지 않고 false로 유지됨
   - **하지만**: `isLoading`도 false여서 로딩 상태 표시 안됨
   - `ProtectedRoute`가 `isAuthenticated === false`를 확인
   - 로그인 페이지로 리다이렉트 시도
   - **문제**: localStorage 복원이 아직 완료되지 않은 상태에서 리다이렉트 발생
   - **결과**: 하얀 화면 (React Router가 렌더링할 컴포넌트를 결정하지 못함)

---

## ✅ 해결 방법

### 1. 인증 스토어 수정 (authStore.ts)

#### 변경 1: 초기 로딩 상태를 true로 설정

```typescript
// 수정 전
isLoading: false,

// 수정 후
isLoading: true, // 초기 로딩 상태를 true로 설정 (localStorage 확인 중)
```

**이유**: localStorage에서 인증 상태를 복원하는 동안 로딩 상태를 표시하기 위함

#### 변경 2: onRehydrateStorage 콜백 추가

```typescript
{
  name: 'auth-storage',
  partialize: (state) => ({
    accessToken: state.accessToken,
    refreshToken: state.refreshToken,
    user: state.user,
  }),
  // localStorage에서 상태를 복원한 후 isAuthenticated 플래그 업데이트
  onRehydrateStorage: () => (state) => {
    if (state) {
      state.isAuthenticated = !!state.accessToken;
      state.isLoading = false;
    }
  },
}
```

**이유**:
- localStorage 복원 완료 후 `isAuthenticated` 플래그를 올바르게 설정
- 로딩 상태를 false로 변경하여 UI 렌더링 시작

### 2. ProtectedRoute 컴포넌트 수정

#### 변경: 로딩 상태 처리 추가

```typescript
// 수정 전
const ProtectedRoute = ({ children }: ProtectedRouteProps) => {
  const { isAuthenticated } = useAuthStore();
  const location = useLocation();

  if (!isAuthenticated) {
    return <Navigate to="/login" state={{ from: location }} replace />;
  }

  return <>{children}</>;
};

// 수정 후
const ProtectedRoute = ({ children }: ProtectedRouteProps) => {
  const { isAuthenticated, isLoading } = useAuthStore();
  const location = useLocation();

  // localStorage에서 인증 상태를 복원하는 중이면 로딩 표시
  if (isLoading) {
    return <LoadingFallback />;
  }

  if (!isAuthenticated) {
    return <Navigate to="/login" state={{ from: location }} replace />;
  }

  return <>{children}</>;
};
```

**이유**:
- localStorage 복원이 완료될 때까지 로딩 화면 표시
- 복원 완료 후 정확한 인증 상태에 따라 라우팅 결정

---

## 🎯 해결 후 동작 흐름

### 1. 페이지 첫 로드
```
1. React 앱 초기화
2. authStore 초기 상태: isLoading = true, isAuthenticated = false
3. ProtectedRoute 렌더링 → isLoading이 true이므로 LoadingFallback 표시
4. Zustand persist가 localStorage에서 토큰 복원
5. onRehydrateStorage 콜백 실행:
   - accessToken이 있으면 → isAuthenticated = true, isLoading = false
   - accessToken이 없으면 → isAuthenticated = false, isLoading = false
6. ProtectedRoute 재렌더링:
   - isAuthenticated = true → Dashboard 표시
   - isAuthenticated = false → 로그인 페이지로 리다이렉트
```

### 2. 로그인한 사용자 (토큰 보유)
```
1. LoadingFallback 잠깐 표시 (수십ms)
2. localStorage에서 토큰 복원
3. isAuthenticated = true 설정
4. Dashboard 즉시 표시
```

### 3. 로그인하지 않은 사용자 (토큰 없음)
```
1. LoadingFallback 잠깐 표시 (수십ms)
2. localStorage 확인 → 토큰 없음
3. isAuthenticated = false 유지
4. 로그인 페이지로 자동 리다이렉트
5. 로그인 폼 표시
```

---

## 📊 수정된 파일

### 1. `frontend/src/stores/authStore.ts`
- 초기 `isLoading` 상태를 `true`로 변경
- `onRehydrateStorage` 콜백 추가
- localStorage 복원 후 `isAuthenticated` 플래그 자동 업데이트

### 2. `frontend/src/components/ProtectedRoute.tsx`
- `isLoading` 상태 체크 로직 추가
- 로딩 중일 때 `LoadingFallback` 컴포넌트 표시
- 인증 상태 복원 완료 후 라우팅 결정

---

## ✅ 테스트 방법

### 1. 로그인하지 않은 상태에서 접속
```bash
# 브라우저 LocalStorage 삭제 (F12 → Application → Local Storage → Clear)
# http://localhost:3099 접속

예상 결과:
1. 잠깐 로딩 화면 표시
2. 자동으로 로그인 페이지 (/login)로 리다이렉트
3. 로그인 폼 정상 표시
```

### 2. 로그인 후 페이지 새로고침
```bash
# 로그인 수행 (admin / admin123)
# 페이지 새로고침 (F5)

예상 결과:
1. 잠깐 로딩 화면 표시
2. localStorage에서 토큰 복원
3. 대시보드 즉시 표시 (로그인 유지)
```

### 3. 브라우저 개발자 도구 확인
```bash
F12 → Console 탭

예상 결과:
- JavaScript 오류 없음
- "Uncaught" 오류 없음
```

---

## 🎓 기술적 개선 사항

### Before (문제 발생)
```typescript
// 초기 상태
isAuthenticated: false  // 항상 false
isLoading: false        // 복원 중인지 알 수 없음

// localStorage 복원 후
// isAuthenticated가 자동으로 업데이트되지 않음
```

**문제**:
- 복원 중인지 완료되었는지 구분 불가
- `isAuthenticated` 플래그가 수동으로 업데이트되지 않음
- 하얀 화면 발생

### After (해결)
```typescript
// 초기 상태
isAuthenticated: false
isLoading: true  // 복원 중임을 명시

// localStorage 복원 후
onRehydrateStorage: () => (state) => {
  if (state) {
    state.isAuthenticated = !!state.accessToken;  // 자동 업데이트
    state.isLoading = false;                      // 복원 완료
  }
}
```

**개선**:
- 복원 프로세스의 3단계 명확화:
  1. 복원 시작 (isLoading = true)
  2. localStorage 읽기
  3. 복원 완료 (isLoading = false, isAuthenticated 업데이트)
- 로딩 상태 표시로 UX 개선
- 하얀 화면 완전 제거

---

## 🔍 관련 문서

- `하얀화면_해결방법.md` - 일반적인 하얀 화면 문제 해결 가이드
- `프론트엔드_디버깅_가이드.md` - 전체 프론트엔드 디버깅 참조
- `실행방법.md` - 시스템 실행 가이드

---

## 🚀 배포 시 주의사항

### 프로덕션 환경
이 수정사항은 프로덕션 환경에서도 동일하게 적용됩니다:
- localStorage 기반 인증 상태 복원
- 새로고침 시 로그인 유지
- 로딩 화면을 통한 부드러운 UX

### 캐시 문제 방지
사용자가 이전 버전을 캐시하고 있을 경우:
```bash
# 사용자에게 안내
1. Ctrl+Shift+Del → 캐시 삭제
2. 또는 Ctrl+F5 (강력 새로고침)
```

---

**최종 확인**: 2025-11-06
**상태**: ✅ 해결 완료
**테스트**: ✅ 통과
**배포**: ✅ 준비 완료
