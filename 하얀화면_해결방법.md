# 프론트엔드 하얀 화면 문제 - 즉시 해결 가이드

**문제**: http://localhost:3099 접속 시 하얀 화면만 표시됨

---

## ⚡ 즉시 해결 방법

### 1단계: 브라우저 개발자 도구 열기

**Windows/Linux**:
- `F12` 키 누르기
- 또는 `Ctrl + Shift + I`

**Mac**:
- `Cmd + Option + I`

### 2단계: Console 탭 확인

브라우저 개발자 도구 하단의 **Console** 탭을 클릭하고 오류 메시지를 확인하세요.

#### 예상되는 오류들:

---

## 🔴 오류 1: "Failed to fetch" 또는 "Network Error"

### 증상:
```
Failed to fetch
NetworkError when attempting to fetch resource
```

### 원인:
Backend 서버가 실행되지 않았거나, 포트가 맞지 않습니다.

### 해결책:

#### 1️⃣ Backend 서버 실행 확인
```cmd
# 새 터미널에서 실행
curl http://localhost:8099/health
```

**정상 응답**:
```json
{"status":"healthy"}
```

**오류 발생 시**:
```cmd
cd backend
run_backend.bat
```

#### 2️⃣ Backend 서버가 정상 실행되면:
브라우저에서 `Ctrl+F5` (강력 새로고침)

---

## 🟡 오류 2: "CORS policy" 오류

### 증상:
```
Access to fetch at 'http://localhost:8099/api/v1/...' from origin 'http://localhost:3099' has been blocked by CORS policy
```

### 원인:
Backend CORS 설정에 Frontend 포트가 등록되지 않았습니다.

### 해결책:

이 오류는 이미 해결되어 있어야 합니다. 만약 여전히 발생한다면:

```cmd
# Backend 서버 재시작
cd backend
(기존 서버 Ctrl+C로 종료)
run_backend.bat
```

---

## 🔵 오류 3: "Cannot find module" 또는 import 오류

### 증상:
```
Error: Cannot find module '@mui/material'
Failed to resolve import
```

### 원인:
Frontend 의존성 패키지가 설치되지 않았습니다.

### 해결책:

```cmd
cd frontend

# node_modules 삭제
rmdir /s /q node_modules
del /f /q package-lock.json

# 재설치
install_frontend.bat

# 서버 재시작
run_frontend.bat
```

---

## 🟢 오류 4: 아무 오류도 없지만 하얀 화면

### 원인:
React 컴포넌트 렌더링 문제 또는 라우팅 문제

### 해결책:

#### 1️⃣ 로그인 페이지로 직접 이동
브라우저 주소창에 다음을 입력:
```
http://localhost:3099/login
```

**정상 작동**: 로그인 폼이 표시됨
**여전히 하얀 화면**: 다음 단계로

#### 2️⃣ 브라우저 캐시 완전 삭제

**Chrome/Edge**:
1. `Ctrl+Shift+Del` 누르기
2. "전체 기간" 선택
3. "캐시된 이미지 및 파일" 체크
4. "쿠키 및 기타 사이트 데이터" 체크
5. "데이터 삭제" 클릭
6. 브라우저 완전 종료 후 재시작
7. http://localhost:3099 접속

#### 3️⃣ 시크릿 모드로 테스트

**Chrome/Edge**:
1. `Ctrl+Shift+N` 누르기
2. http://localhost:3099 접속

**정상 작동**: 캐시 문제 확정
**여전히 하얀 화면**: Frontend 서버 재시작 필요

#### 4️⃣ Frontend 서버 완전 재시작

```cmd
# 현재 실행 중인 Frontend 서버 종료 (Ctrl+C)

cd frontend
run_frontend.bat

# 또는 수동 재시작
npm run dev
```

---

## 🎯 정상 작동 확인 방법

### 1. Backend 확인
```cmd
curl http://localhost:8099/health
```
**응답**: `{"status":"healthy"}`

### 2. Frontend 확인
브라우저에서 http://localhost:3099 접속

**정상 화면**: 로그인 폼이 표시됨

### 3. 로그인 테스트
- **Username**: admin
- **Email**: admin@localhost.com
- **Password**: admin123

입력 후 "로그인" 버튼 클릭

**정상 작동**: 대시보드로 이동

---

## 🔧 고급 문제 해결

### 상황 1: 무한 리다이렉트 (화면이 깜빡거림)

#### 증상:
- 화면이 계속 깜빡거리며 로딩됨
- 주소창의 URL이 계속 변경됨

#### 해결:
```cmd
# 브라우저 LocalStorage 삭제
```

**Chrome/Edge**:
1. 개발자 도구(`F12`) 열기
2. **Application** 탭 클릭
3. 왼쪽 **Local Storage** → `http://localhost:3099` 클릭
4. 모든 항목 삭제 (우클릭 → Clear)
5. 페이지 새로고침

### 상황 2: "Loading..." 화면에서 멈춤

#### 증상:
- 로딩 아이콘만 계속 돌아감
- 아무 오류 메시지도 없음

#### 원인:
- Backend API 응답 없음
- 네트워크 타임아웃

#### 해결:

**1단계**: Backend API 직접 테스트
```cmd
curl http://localhost:8099/api/v1/auth/me
```

**예상 응답** (인증 안됨):
```json
{"detail":"Not authenticated"}
```

**응답 없음**: Backend 서버 문제 → Backend 재시작

**2단계**: Network 탭 확인
1. 개발자 도구(`F12`) → **Network** 탭
2. 페이지 새로고침
3. 빨간색으로 표시된 실패한 요청 확인
4. 클릭하여 오류 메시지 확인

### 상황 3: TypeScript 컴파일 오류

#### 증상:
Terminal에 TypeScript 오류 메시지 표시

#### 해결:
```cmd
cd frontend

# TypeScript 캐시 삭제
rmdir /s /q node_modules/.vite

# 서버 재시작
npm run dev
```

---

## 📝 완전 초기화 (모든 방법이 실패한 경우)

### 1단계: Backend 완전 재설치

```cmd
cd backend

# 가상환경 삭제
rmdir /s /q venv

# 재설치
install_backend.bat

# 실행
run_backend.bat
```

### 2단계: Frontend 완전 재설치

```cmd
cd frontend

# node_modules 삭제
rmdir /s /q node_modules
rmdir /s /q node_modules/.vite
del /f /q package-lock.json

# 재설치
install_frontend.bat

# 실행
run_frontend.bat
```

### 3단계: 브라우저 데이터 완전 삭제

1. 브라우저 설정 → 개인정보 보호
2. "인터넷 사용 기록 삭제" 또는 "사이트 데이터 삭제"
3. **모든 항목** 선택 후 삭제
4. 브라우저 재시작

---

## 🎓 브라우저 개발자 도구 사용법

### Console 탭 (가장 중요!)
- JavaScript 오류 메시지 확인
- 빨간색 메시지: 오류
- 노란색 메시지: 경고

### Network 탭
- API 요청/응답 확인
- 빨간색: 실패한 요청
- 200-299: 성공
- 400-499: 클라이언트 오류
- 500-599: 서버 오류

### Application 탭
- LocalStorage: 로그인 토큰 저장소
- Cookies: 쿠키 정보
- Cache Storage: 캐시 데이터

---

## 📞 추가 도움

### 오류 로그 수집 방법

#### Backend 로그:
Terminal 출력 전체 복사

#### Frontend 로그:
1. Terminal 출력 전체 복사
2. 브라우저 Console 전체 복사 (`Ctrl+A` → `Ctrl+C`)
3. 브라우저 Network 탭 스크린샷

### 시스템 정보 확인:
```cmd
# Python 버전
python --version

# Node.js 버전
node --version

# npm 버전
npm --version
```

---

## ✅ 체크리스트

완전히 정상 작동하는 시스템:

- [ ] Backend 서버 실행 중 (http://localhost:8099/health 응답)
- [ ] Frontend 서버 실행 중 (Terminal에 "VITE ready" 표시)
- [ ] http://localhost:3099 접속 시 로그인 폼 표시
- [ ] 브라우저 Console에 오류 없음
- [ ] 브라우저 Network 탭에 빨간색 요청 없음
- [ ] 로그인 가능 (admin / admin123)
- [ ] 로그인 후 대시보드 표시

---

**마지막 업데이트**: 2025-11-06
**버전**: v2.0.0
**포트**: Backend 8099, Frontend 3099
