# 폐쇄망 프로젝트 관리 시스템 v2.0.0 실행 방법

**버전**: v2.0.0
**최종 업데이트**: 2025-11-06

---

## 📋 목차

1. [시스템 요구사항](#-시스템-요구사항)
2. [초기 설치 및 설정](#-초기-설치-및-설정)
3. [서버 실행 방법](#-서버-실행-방법)
4. [접속 방법](#-접속-방법)
5. [기본 사용 방법](#-기본-사용-방법)
6. [문제 해결](#-문제-해결)

---

## 💻 시스템 요구사항

### 필수 요구사항
- **Python**: 3.10 이상
- **Node.js**: 18 이상
- **npm**: 9 이상
- **운영체제**: Windows 10/11, macOS, Linux (WSL2 포함)

### 권장 사양
- RAM: 최소 4GB (권장 8GB)
- 디스크 여유 공간: 최소 500MB

---

## 🔧 초기 설치 및 설정

### 1단계: 프로젝트 클론
```bash
git clone <repository-url>
cd Project_Master
```

### 2단계: Backend 설정
```bash
cd backend

# Python 가상환경 생성
python3 -m venv venv

# 가상환경 활성화 (Windows)
venv\Scripts\activate

# 가상환경 활성화 (macOS/Linux/WSL)
source venv/bin/activate

# 의존성 패키지 설치
pip install -r requirements.txt

# bcrypt 버전 조정 (passlib 호환성)
pip install bcrypt==3.2.0
```

**중요**: bcrypt는 반드시 3.2.0 버전을 사용해야 합니다. (passlib 1.7.4와 호환성 문제)

### 3단계: Frontend 설정
```bash
cd frontend

# 의존성 패키지 설치
npm install
```

---

## 🚀 서버 실행 방법

### Backend 서버 실행

**터미널 1**에서 실행:

```bash
cd backend

# 가상환경 활성화 (활성화 안 된 경우)
source venv/bin/activate  # macOS/Linux/WSL
# 또는
venv\Scripts\activate     # Windows

# 서버 시작
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

**성공 메시지**:
```
INFO:     Uvicorn running on http://0.0.0.0:8000 (Press CTRL+C to quit)
INFO:     Started reloader process [PID] using WatchFiles
Server initialized for asgi.
INFO:     Started server process [PID]
INFO:     Waiting for application startup.
INFO:     Application startup complete.
```

### Frontend 서버 실행

**터미널 2**에서 실행:

```bash
cd frontend

# 개발 서버 시작
npm run dev
```

**성공 메시지**:
```
  VITE v7.1.12  ready in XXXX ms

  ➜  Local:   http://localhost:3000/
  ➜  Network: http://XXX.XXX.XXX.XXX:3000/
```

---

## 🌐 접속 방법

### 1. Swagger API 문서
- **URL**: http://localhost:8000/docs
- **용도**: API 엔드포인트 테스트, 문서 확인
- **특징**: 인터랙티브 API 테스팅 가능

### 2. Frontend 웹 애플리케이션
- **URL**: http://localhost:3000
- **용도**: 실제 사용자 인터페이스
- **브라우저**: Chrome, Firefox, Edge, Safari 최신 버전 권장

### 3. Backend API 직접 접근
- **URL**: http://localhost:8000
- **Health Check**: http://localhost:8000/health
- **API Base URL**: http://localhost:8000/api/v1

---

## 📱 기본 사용 방법

### 1. 회원가입 및 로그인

#### Swagger UI에서 테스트:
1. http://localhost:8000/docs 접속
2. `POST /api/v1/auth/register` 엔드포인트 클릭
3. **Try it out** 버튼 클릭
4. 아래 예제 데이터 입력:
```json
{
  "username": "admin",
  "email": "admin@localhost.com",
  "password": "admin123",
  "full_name": "System Admin"
}
```
5. **Execute** 버튼 클릭

#### 로그인:
1. `POST /api/v1/auth/login` 엔드포인트 클릭
2. **Try it out** 버튼 클릭
3. Form Data 입력:
   - `username`: admin
   - `password`: admin123
4. **Execute** 버튼 클릭
5. **Response Body**에서 `access_token` 복사

#### 인증 토큰 사용:
1. 페이지 상단의 **Authorize** 버튼 클릭
2. Value 입력란에 `Bearer <your-access-token>` 입력
3. **Authorize** 버튼 클릭
4. 이제 인증이 필요한 모든 API 사용 가능

### 2. 프로젝트 생성

#### Swagger UI에서:
1. `POST /api/v1/projects/` 엔드포인트 클릭
2. **Try it out** 버튼 클릭
3. Request Body 입력:
```json
{
  "name": "신규 프로젝트",
  "description": "프로젝트 설명",
  "start_date": "2025-11-06T00:00:00",
  "end_date": "2025-12-31T23:59:59",
  "status": "in_progress"
}
```
4. **Execute** 버튼 클릭

#### curl 명령어로:
```bash
# 로그인하여 토큰 획득
TOKEN=$(curl -s -X POST http://localhost:8000/api/v1/auth/login \
  -H "Content-Type: application/x-www-form-urlencoded" \
  -d "username=admin&password=admin123" \
  | python3 -c "import json,sys; print(json.load(sys.stdin)['access_token'])")

# 프로젝트 생성
curl -X POST "http://localhost:8000/api/v1/projects/" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{
    "name": "테스트 프로젝트",
    "description": "API 테스트용 프로젝트",
    "start_date": "2025-11-06T00:00:00",
    "end_date": "2025-12-31T23:59:59",
    "status": "in_progress"
  }'
```

### 3. 프로젝트 조회
```bash
# 모든 프로젝트 조회
curl -X GET "http://localhost:8000/api/v1/projects/" \
  -H "Authorization: Bearer $TOKEN"
```

---

## 🐛 문제 해결

### Backend 서버 시작 실패

#### 증상 1: `ModuleNotFoundError: No module named 'socketio'`
**해결 방법**:
```bash
pip install python-socketio python-socketio[asyncio]
```

#### 증상 2: `ModuleNotFoundError: No module named 'email_validator'`
**해결 방법**:
```bash
pip install email-validator
```

#### 증상 3: bcrypt 호환성 오류
```
AttributeError: module 'bcrypt' has no attribute '__about__'
```
**해결 방법**:
```bash
pip install bcrypt==3.2.0
```

#### 증상 4: 포트 8000이 이미 사용 중
**해결 방법**:
```bash
# 포트를 사용 중인 프로세스 확인 (Linux/macOS)
lsof -i :8000

# 프로세스 종료
kill -9 <PID>

# Windows에서
netstat -ano | findstr :8000
taskkill /PID <PID> /F
```

### Frontend 서버 시작 실패

#### 증상 1: `node_modules` 누락
**해결 방법**:
```bash
cd frontend
npm install
```

#### 증상 2: 포트 3000이 이미 사용 중
**해결 방법**:
```bash
# 포트 변경 (vite.config.ts 수정)
# 또는 프로세스 종료
lsof -i :3000  # Linux/macOS
kill -9 <PID>
```

### CORS 오류

#### 증상: `Access to XMLHttpRequest has been blocked by CORS policy`
**원인**: Backend의 CORS 설정과 Frontend 포트 불일치

**해결 방법**:
1. `backend/app/main.py` 파일에서 CORS 설정 확인
2. Frontend 실제 포트(3000)가 `allow_origins`에 포함되어 있는지 확인

### 데이터베이스 오류

#### 증상: `no such table: users`
**원인**: 데이터베이스가 초기화되지 않음

**해결 방법**: 첫 API 호출 (회원가입)을 하면 자동으로 생성됩니다.

**데이터베이스 위치**: `backend/data/project_manager.db`

---

## 📚 추가 정보

### API 엔드포인트 목록

총 **36개** 엔드포인트가 제공됩니다:

#### Authentication (5개)
- `POST /api/v1/auth/register` - 회원가입
- `POST /api/v1/auth/login` - 로그인
- `POST /api/v1/auth/logout` - 로그아웃
- `GET /api/v1/auth/me` - 현재 사용자 조회
- `POST /api/v1/auth/refresh` - 토큰 갱신

#### Users (사용자 관리)
- 사용자 CRUD 엔드포인트

#### Projects (프로젝트 관리)
- `GET /api/v1/projects/` - 프로젝트 목록
- `POST /api/v1/projects/` - 프로젝트 생성
- `GET /api/v1/projects/{id}` - 프로젝트 조회
- `PUT /api/v1/projects/{id}` - 프로젝트 수정
- `DELETE /api/v1/projects/{id}` - 프로젝트 삭제

#### Tasks (태스크 관리)
- 태스크 CRUD 엔드포인트

#### Enablers (이네이블러 관리)
- 이네이블러 CRUD 엔드포인트

#### Dependencies (의존성 관리)
- 의존성 CRUD 엔드포인트

#### Attachments (파일 첨부)
- `POST /api/v1/attachments/upload` - 파일 업로드
- `GET /api/v1/attachments/{id}/download` - 파일 다운로드

#### Notifications (알림)
- `GET /api/v1/notifications/` - 알림 목록
- `POST /api/v1/notifications/read-all` - 모두 읽음 처리
- `GET /api/v1/notifications/unread-count` - 안 읽은 알림 개수

**상세 문서**: http://localhost:8000/docs

---

## 🔧 개발 환경 설정

### Backend 개발
```bash
cd backend

# 개발용 의존성 설치 (이미 requirements.txt에 포함됨)
pip install pytest pytest-cov pytest-asyncio httpx black isort mypy flake8

# 테스트 실행
pytest

# 코드 포맷팅
black app/
isort app/

# 타입 체크
mypy app/
```

### Frontend 개발
```bash
cd frontend

# 타입 체크
npm run build  # TypeScript 컴파일

# 프로덕션 빌드
npm run build

# 프로덕션 빌드 프리뷰
npm run preview
```

---

## 📞 지원

문제가 계속 발생하는 경우:
1. GitHub Issues에 문제 보고
2. `TEST_RESULTS.md` 파일 참조
3. Swagger API 문서에서 엔드포인트 확인

---

**마지막 업데이트**: 2025-11-06
**버전**: v2.0.0
