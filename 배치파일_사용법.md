# Windows 배치 파일 사용법

**폐쇄망 프로젝트 관리 시스템 v2.0.0**

---

## 📋 목차

1. [배치 파일 목록](#-배치-파일-목록)
2. [최초 설치 방법](#-최초-설치-방법)
3. [서버 실행 방법](#-서버-실행-방법)
4. [문제 해결](#-문제-해결)

---

## 📁 배치 파일 목록

### Backend (backend 폴더)
| 파일명 | 용도 | 실행 빈도 |
|--------|------|-----------|
| `install_backend.bat` | Backend 설치 | 최초 1회 |
| `run_backend.bat` | Backend 서버 실행 | 매번 |

### Frontend (frontend 폴더)
| 파일명 | 용도 | 실행 빈도 |
|--------|------|-----------|
| `install_frontend.bat` | Frontend 설치 | 최초 1회 |
| `run_frontend.bat` | Frontend 서버 실행 | 매번 |

---

## 🚀 최초 설치 방법

### 사전 준비

1. **Python 3.10 이상 설치**
   - https://www.python.org/downloads/
   - 설치 시 "Add Python to PATH" 체크 필수!

2. **Node.js 18 이상 설치**
   - https://nodejs.org/
   - LTS 버전 권장

### 1단계: Backend 설치

```cmd
1. Windows 탐색기에서 Project_Master\backend 폴더로 이동
2. install_backend.bat 파일을 더블클릭
3. 설치 완료까지 대기 (약 3-5분)
```

**설치 과정**:
```
[1/6] Python 설치 확인 중...
[2/6] 가상환경 생성 중...
[3/6] 가상환경 활성화 중...
[4/6] pip 업그레이드 중...
[5/6] 의존성 패키지 설치 중...
[6/6] bcrypt 버전 조정 중...
```

**성공 메시지**:
```
========================================
 Backend 설치가 완료되었습니다!
========================================
```

### 2단계: Frontend 설치

```cmd
1. Windows 탐색기에서 Project_Master\frontend 폴더로 이동
2. install_frontend.bat 파일을 더블클릭
3. 설치 완료까지 대기 (약 2-3분)
```

**설치 과정**:
```
[1/3] Node.js 설치 확인 중...
[2/3] npm 설치 확인 중...
[3/3] npm 패키지 설치 중...
```

**성공 메시지**:
```
========================================
 Frontend 설치가 완료되었습니다!
========================================
```

---

## 💻 서버 실행 방법

### 방법 1: 배치 파일 사용 (권장)

#### 1단계: Backend 서버 실행

```cmd
1. Project_Master\backend 폴더로 이동
2. run_backend.bat 더블클릭
3. 서버 시작 확인
```

**성공 화면**:
```
========================================
 Backend 서버를 시작합니다
========================================

접속 정보:
  - API 서버: http://localhost:8000
  - Swagger UI: http://localhost:8000/docs
  - Health Check: http://localhost:8000/health

서버를 종료하려면 Ctrl+C를 누르세요.
========================================

INFO:     Uvicorn running on http://0.0.0.0:8000
INFO:     Application startup complete.
```

#### 2단계: Frontend 서버 실행

**새 터미널 창을 열어서:**

```cmd
1. Project_Master\frontend 폴더로 이동
2. run_frontend.bat 더블클릭
3. 브라우저가 자동으로 열림
```

**성공 화면**:
```
========================================
 Frontend 개발 서버를 시작합니다
========================================

접속 정보:
  - Frontend: http://localhost:3000
  - Backend API: http://localhost:8000

브라우저가 자동으로 열립니다...
========================================

VITE v7.1.12  ready in XXX ms
➜  Local:   http://localhost:3000/
```

### 방법 2: 명령 프롬프트에서 직접 실행

#### Backend 실행

```cmd
cd C:\Programming\Project_Master\backend
venv\Scripts\activate
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

#### Frontend 실행

```cmd
cd C:\Programming\Project_Master\frontend
npm run dev
```

---

## 🌐 접속 방법

### Backend (API 서버)

| URL | 용도 |
|-----|------|
| http://localhost:8000 | API 기본 주소 |
| http://localhost:8000/docs | Swagger API 문서 (대화형 테스트 가능) |
| http://localhost:8000/health | 서버 상태 확인 |

### Frontend (웹 애플리케이션)

| URL | 용도 |
|-----|------|
| http://localhost:3000 | 메인 웹 애플리케이션 |

---

## 🎮 사용 예시

### 1. Swagger UI에서 API 테스트

1. 브라우저에서 http://localhost:8000/docs 접속
2. `POST /api/v1/auth/register` 클릭
3. "Try it out" 버튼 클릭
4. 회원가입 정보 입력:
```json
{
  "username": "admin",
  "email": "admin@localhost.com",
  "password": "admin123",
  "full_name": "관리자"
}
```
5. "Execute" 버튼 클릭
6. Response에서 회원가입 성공 확인

### 2. Frontend에서 로그인

1. http://localhost:3000 접속
2. 회원가입 또는 로그인
3. 프로젝트 관리 시작!

---

## 🐛 문제 해결

### Backend 문제

#### 1. "Python이 설치되어 있지 않습니다"
**해결 방법**:
- Python 3.10 이상 설치
- 설치 시 "Add Python to PATH" 체크
- 설치 후 명령 프롬프트를 재시작

#### 2. "포트 8000이 이미 사용 중입니다"
**해결 방법**:
```cmd
# 포트 사용 중인 프로세스 확인
netstat -ano | findstr :8000

# 프로세스 종료 (PID 확인 후)
taskkill /F /PID <PID>
```

또는 배치 파일 실행 시 자동으로 물어봅니다:
```
기존 프로세스를 종료하고 계속하시겠습니까? [Y,N]
```

#### 3. "bcrypt 호환성 오류"
**증상**:
```
AttributeError: module 'bcrypt' has no attribute '__about__'
```

**해결 방법**:
```cmd
cd backend
venv\Scripts\activate
pip uninstall bcrypt
pip install bcrypt==3.2.0
```

#### 4. "ModuleNotFoundError: No module named 'socketio'"
**해결 방법**:
```cmd
cd backend
venv\Scripts\activate
pip install python-socketio python-engineio
```

### Frontend 문제

#### 1. "Node.js가 설치되어 있지 않습니다"
**해결 방법**:
- Node.js 18 이상 설치 (https://nodejs.org/)
- 설치 후 명령 프롬프트를 재시작

#### 2. "npm 패키지 설치 실패"
**해결 방법**:
```cmd
# 캐시 삭제
npm cache clean --force

# node_modules 삭제
cd frontend
rmdir /s /q node_modules
del /f /q package-lock.json

# 재설치
install_frontend.bat
```

#### 3. "포트 3000이 이미 사용 중입니다"
**해결 방법**:
```cmd
# 포트 사용 중인 프로세스 확인
netstat -ano | findstr :3000

# 프로세스 종료
taskkill /F /PID <PID>
```

#### 4. "Backend 서버가 실행 중이지 않습니다"
**해결 방법**:
1. 먼저 Backend 서버를 실행 (`run_backend.bat`)
2. Backend가 정상 실행되는지 확인 (http://localhost:8000/health)
3. 그 다음 Frontend 서버 실행

---

## 🔄 서버 재시작 방법

### Backend 재시작

```cmd
1. 현재 실행 중인 창에서 Ctrl+C 누름
2. run_backend.bat 다시 실행
```

### Frontend 재시작

```cmd
1. 현재 실행 중인 창에서 Ctrl+C 누름
2. run_frontend.bat 다시 실행
```

---

## 📝 일상적인 사용 흐름

### 매일 작업 시작

```
1. run_backend.bat 실행 (backend 폴더)
2. run_frontend.bat 실행 (frontend 폴더)
3. 브라우저에서 http://localhost:3000 접속
4. 작업 시작!
```

### 작업 종료

```
1. Frontend 창에서 Ctrl+C
2. Backend 창에서 Ctrl+C
3. 모든 터미널 창 닫기
```

---

## 🎯 핵심 요약

### 최초 1회 (설치)
```
backend/install_backend.bat
frontend/install_frontend.bat
```

### 매번 실행
```
backend/run_backend.bat  (먼저 실행)
frontend/run_frontend.bat (그 다음 실행)
```

### 접속
```
Frontend: http://localhost:3000
API 문서: http://localhost:8000/docs
```

---

## 📞 추가 도움말

### 관련 문서
- `실행방법.md` - 상세 실행 가이드
- `backend/설치가이드.md` - Backend 설치 상세 가이드
- `TEST_RESULTS.md` - 테스트 결과 및 문제 해결

### 테스트 계정
- Username: `admin`
- Email: `admin@localhost.com`
- Password: `admin123`

---

**마지막 업데이트**: 2025-11-06
**버전**: v2.0.0
